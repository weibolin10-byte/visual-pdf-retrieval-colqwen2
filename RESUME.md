# 简历与面试表述

## 项目名称

基于 ColQwen2 Late Interaction 的多领域 PDF 视觉检索与多模态 RAG 系统

## 简历版本

- 基于 ColQwen2 构建无需 OCR 的 PDF 页面级视觉检索系统，将论文、财报、公共报告和技术手册等 30 份文档共 2312 页编码为多向量索引，并通过 MaxSim Late Interaction 完成跨模态查询匹配；RTX 5090 上索引构建耗时 200.5 秒。
- 提前确定 40 条问题及其准确页码，覆盖图表、财务表格、架构图、引脚图和机械尺寸图；ColQwen2 达到 Page Recall@1 0.90、Recall@5 1.00、MRR 0.9437，相较 PDF 原生文字层 BM25 和真实 EasyOCR+BM25，Recall@1 分别提升 12.5 和 15 个百分点。
- 构建 ColQwen2、PDF文字层BM25、EasyOCR+BM25 三路对照测试，并集成 Qwen2.5-VL 与页码引用；在 16 条答案评测中，将 12 个可回答问题拆成 61 个关键事实，事实正确率 75.41%，4 个语料外问题全部正确拒答，同时定位复杂表格错行和错误页引用等主要失败模式。

## 技术栈

Python、PyTorch、Transformers、ColQwen2、Qwen2.5-VL、Late Interaction、EasyOCR、BM25、PyMuPDF、Gradio、CUDA

## 一分钟项目介绍

传统 PDF RAG 通常先做 OCR 或提取文本，再切块和检索，这会丢失图表、页面布局和图中元素之间的空间关系。这个项目使用 ColQwen2 直接把完整 PDF 页面图像编码成多向量表示，再使用类似 ColBERT 的 Late Interaction，让每个查询 token 与页面中的最佳视觉 token 匹配。系统在 30 份跨领域 PDF、2312 页语料上完成零样本检索，没有训练或微调模型。

为了验证提升是否只是来自“读出了图片中的文字”，我提前确定了同一套 40 条精确页码问题，同时实现 PDF 原生文字层 BM25 和真实 EasyOCR+BM25。ColQwen2 的 Page Recall@1 为 0.90，两种 BM25 分别为 0.775 和 0.75；引脚图、机械尺寸图和漫画式页面中，视觉检索的排名优势尤其明显。最后使用 Qwen2.5-VL 读取召回页面并生成带 PDF 文件名和页码引用的中文答案，并逐项核对 61 个关键事实，而不是只检查答案格式。

## 面试中需要主动说明

- 项目没有训练或微调模型，贡献点是完整工程复现、跨领域语料构建、严格页面级评测、真实 OCR 对照、失败案例分析和可验证演示系统。
- 文档级 Recall@1 为 1.00，但这是闭集文档路由指标；更严格的精确页面级 Recall@1 为 0.90。
- 页面级评测有 40 条人工问题，答案质量评测有 16 条，因此不宣称统计显著性或通用 SOTA。
- 生成阶段的关键事实正确率为 75.41%，说明检索结果很好不等于最终答案完全可靠；复杂表格的行列对应仍需改进。
- Qwen2.5-VL 是召回后的回答生成器；ColQwen2 才是核心页面检索模型。

## 常见追问

### Late Interaction 怎么计算？

查询和页面都保留多向量表示。对每个查询向量，在页面所有视觉向量中寻找最大点积，再将这些最大值求和：

\[
s(q,p)=\sum_i\max_j\langle q_i,p_j\rangle
\]

它比把整页压成单一向量保留了更细粒度的 token/patch 匹配信息，同时允许提前离线编码页面。

### 为什么还要实现 OCR+BM25？

因为只与 PDF 文本层比较，无法排除视觉模型仅仅具备 OCR 能力。EasyOCR+BM25 从 JPG 页面重新识别文字，形成独立的真实 OCR 基线；结果表明即使 OCR 文本非空率达到 99.09%，它仍弱于 ColQwen2，说明视觉布局信息确实产生了价值。

### 为什么 OCR 在引脚图上表现差？

OCR可以识别部分引脚名称，但输出通常是线性文字序列，会丢失引脚位于芯片哪一侧、编号与标签如何对应等二维几何关系。ColQwen2直接使用页面视觉 patch，因此更容易匹配“某种布局的引脚图”这类查询。
